# EZREC Backend Dependencies (Raspberry Pi Optimized)
# Production-ready with Picamera2 and system integration

# Core Dependencies
python-dotenv==1.0.0
psutil==5.9.8
pytz==2024.1

# Supabase Stack - Main package and storage
supabase==2.2.1
storage3==0.7.7
postgrest==0.12.0
gotrue==1.3.0

# HTTP Client Stack - Compatible versions
httpx==0.24.1
httpcore==0.17.3
h11==0.14.0
certifi==2023.7.22

# Computer Vision & Media Processing
opencv-python==4.8.1.78
numpy==1.26.4

# File handling and utilities
pathlib2==2.3.7
typing-extensions==4.8.0

# Async support
asyncio-mqtt==0.16.1

# CRITICAL: Raspberry Pi System Packages Required
# Install these BEFORE running pip install:
# sudo apt update
# sudo apt install -y python3-libcamera python3-picamera2 python3-opencv
# sudo apt install -y ffmpeg v4l-utils python3-dev
# sudo apt install -y libcamera-apps libcamera-tools

# For exclusive camera access:
# sudo systemctl disable motion
# sudo systemctl disable mjpg-streamer
# sudo systemctl stop motion
# sudo systemctl stop mjpg-streamer

# Note: Picamera2 is installed via system packages, not pip
# This ensures proper integration with Raspberry Pi camera hardware

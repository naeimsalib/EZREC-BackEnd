# EZREC Backend Requirements
# Core dependencies for soccer recording system

# Supabase integration
supabase>=2.0.0
postgrest>=1.0.0

# Web framework and HTTP
requests>=2.25.0
httpx>=0.25.0

# Camera and video processing (Picamera2 ecosystem)
picamera2>=0.3.0
opencv-python>=4.5.0
numpy>=1.21.0
pillow>=8.0.0

# System monitoring and utilities
psutil>=5.8.0
pytz>=2021.1

# Development and logging
python-dotenv>=0.19.0

# Additional Picamera2 dependencies that might be needed
av>=8.0.0
simplejpeg>=1.5.0

# GPIO and hardware interface (for Raspberry Pi)
# Note: RPi.GPIO is typically installed system-wide on Raspberry Pi

# Database and API utilities
pydantic>=1.8.0
typing-extensions>=4.0.0

# File and data handling
tqdm>=4.60.0

# Time and scheduling
python-dateutil>=2.8.0

# JSON schema validation
jsonschema>=3.2.0

# CRITICAL: Raspberry Pi System Packages Required
# Install these BEFORE running pip install:
# sudo apt update
# sudo apt install -y python3-libcamera python3-picamera2 python3-opencv
# sudo apt install -y ffmpeg v4l-utils python3-dev
# sudo apt install -y libcamera-apps libcamera-tools

# For exclusive camera access:
# sudo systemctl disable motion
# sudo systemctl disable mjpg-streamer
# sudo systemctl stop motion
# sudo systemctl stop mjpg-streamer

# Note: Picamera2 is installed via system packages, not pip
# This ensures proper integration with Raspberry Pi camera hardware
